{% extends 'base.html.twig' %}

{% block body %}
    <h1>Invoice creation</h1>

    {{ form_start(form) }}
    {% form_theme form 'form/invoice.html.twig' %}
    {{ form_widget(form) }}
    <input type="submit" value="Create"/>
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('invoice_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
